{
  "name": "mqtt-media-player",
  "private": true,
  "scripts": {
    "start": "meteor --settings settings.json",
    "build-docker": "docker build -t sherlock/mqtt-media-player ."
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "meteor-node-stubs": "~0.2.3",
    "mqtt": "^2.2.1",
    "mqtt-regex": "^1.0.5"
  }
}
